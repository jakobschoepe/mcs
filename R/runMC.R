#' @title Running a Monte Carlo simulation.
#' @description \code{runMC} runs a Monte Carlo simulation.
#' @usage runMC(object)
#' @param object
#' @details
#' @return
#' @note Please note that \code{runMC} was built as part of the design of a Monte Carlo simulation, and therefore serves a special-purpose only.
#' @author Jakob Schöpe
#' @example

runMC <- function(object) {
  # Check passed arguments to smoothly run subsequent commands
  if (!is.list(x = object)) {
    stop("\"object\" must be a list.")
  }
  
  
  
  else {
  }
}
